apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "aiinference.app-name" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "aiinference.app-name" . }}
  strategy:
    rollingUpdate:
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ template "aiinference.app-name" . }}
        release: {{ .Release.Name }}
        datalayer.io/app: {{ template "aiinference.app-name" . }}
      annotations:
        karpenter.sh/do-not-disrupt: "true"      
    spec:
      terminationGracePeriodSeconds: 0
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: role.datalayer.io/api
                operator: In
                values:
                - "true"
      imagePullSecrets:
            - name: {{ .Values.aiinference.registryCredentialsSecret }}
      containers:
        - name: ai-inference
          image: {{ .Values.aiinference.image }}
          imagePullPolicy: {{ .Values.aiinference.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.aiinference.port }}
              protocol: TCP
          {{- if or .Values.aiinference.env .Values.aiinference.envValueFrom }}
          env:
            {{- range $key, $value := .Values.aiinference.envValueFrom }}
            - name: {{ $key }}
              valueFrom: {{- $value | toYaml | nindent 16 }}
            {{- end }}
            {{- range $key, $value := .Values.aiinference.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
#          readinessProbe:
#            httpGet:
#              path: /api/ai-inference/version
#              port: {{ .Values.aiinference.port }}
#            successThreshold: 1
#            failureThreshold: 5
#            initialDelaySeconds: 5
#            periodSeconds: 15
          volumeMounts:
          resources:
{{ toYaml .Values.aiinference.resources | indent 12 }} 
