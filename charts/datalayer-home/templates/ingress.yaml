{{ if eq .Values.home.clusterType "any" -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-root-redirect
  annotations:
    cert-manager.io/acme-challenge-type: http01
    cert-manager.io/cluster-issuer: {{ .Values.home.certificateIssuer }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Release.Namespace }}-redirect-root@kubernetescrd
    traefik.ingress.kubernetes.io/router.tls: 'true'
    nginx.ingress.kubernetes.io/temporal-redirect: {{ .Values.home.redirectUrl }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
  labels:
    app: home
spec:
  ingressClassName: {{ .Values.home.ingressClass }}
  tls:
  - hosts:
    - {{ template "home.runHost" . }}
    secretName: {{ template "home.runHost" . }}-datalayer-home-cert-secret
  rules:
  - host: {{ template "home.runHost" . }}
    http:
      paths:
      - path: /
        pathType: Exact
        backend:
          service:
            name: home-redirect-svc
            port: 
              number: 9700
{{- end }}
