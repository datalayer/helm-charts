{{ if eq .Values.scheduler.clusterType "gke" -}}
apiVersion: cloud.google.com/v1beta1
kind: BackendConfig
metadata:
  name: scheduler-backend-config
spec:
  sessionAffinity:
    affinityType: "GENERATED_COOKIE"
    affinityCookieTtlSec: 9999
{{- end }}
