{{/*
Expand the name of the chart.
*/}}
{{- define "runtime-pools.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 24 | trimSuffix "-" -}}
{{- end -}}

{{/*
Create a default fully qualified app name.
We truncate at 24 chars because some kubernetes name fields are limited to this (by the DNS naming spec).
*/}}
{{- define "runtime-pools.fullname" -}}
{{- $name := default .Chart.Name .Values.nameOverride -}}
{{- printf "%s-%s" .Release.Name $name | trunc 24 | trimSuffix "-" -}}
{{- end -}}

{{/*
Define the name of the client service for runtime-pools.
*/}}
{{- define "runtime-pools.service-name" -}}
{{- printf "%s-%s" (include "runtime-pools.fullname" .) "svc" | trunc 63 | trimSuffix "-" -}}
{{- end -}}
