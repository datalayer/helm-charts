apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "contents.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "contents.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.contents.apiPort }}
      targetPort: http
